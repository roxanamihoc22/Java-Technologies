spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    url: jdbc:postgresql://localhost:5432/PrefSchedule
    password: Roxana123
  sql:
    init:
      mode: never
  jpa:
    hibernate:
      ddl-auto: none
server:
  port: 8080
